# Required
STRAVA_CLIENT_ID=12345
STRAVA_CLIENT_SECRET=secret
# Allowed site for CORS (frontend origin only, no path)
FRONTEND_URI=https://your-frontend.example.com
# Where the backend will redirect AFTER token exchange
FRONTEND_REDIRECT_URI=https://your-frontend.example.com/auth/strava

# Optional
PORT=8080